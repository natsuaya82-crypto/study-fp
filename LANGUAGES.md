# 語学ドリル（インドネシア語・中国語）

FP用アプリと同じ仕組みを語学向けに作り直したものです。
**1つのアプリで2言語**を扱い、ヘッダーの `ID / 中` ボタンで切り替えます。

## アプリ

https://claude.ai/artifact/9czPujrKNu51DmQZgi4R7v

| 言語 | 章 | 問題 | 入力式で答えられる問題 |
|------|----|------|------------------------|
| インドネシア語 | 23 | 417（文法85・単語332） | 182 |
| 中国語 | 23 | 300（文法60・単語240） | 128 |

学習記録は**言語ごとに完全に別**（読了・マーカー・ブックマーク・弱点・得点推移すべて）。
保存は1つの `progress/main` に `{v:2, id:{…}, zh:{…}}` の形で入れています。
旧・単独アプリの記録（`id-drill-v1` / `zh-drill-v1`）は起動時に自動で取り込みます。

## 出題の形式

**入力式（既定）／選択式** を設定で切り替えます。入力式では答えを自分でタイプします。

- 判定は大文字小文字・空白・句読点・**声調記号**を無視して比較（`nǐhǎo` も `NIHAO` も `你好` も正解）
- 中国語は**漢字でも声調なしピンインでも**通る（`inp:['你好','nihao']`）
- 並べ替え問題は、タイルを並べるかわりに文をそのまま入力
- 単語は入力式のとき「日本語→外国語」の**産出方向に自動で差し替え**（`TWIN`）。
  意味を選ぶだけの問題にはならない
- 答えが出てこないときは **「わからない → 選択肢を見る」** で、その1問だけ選択式に戻せる
- 発音・語順・使い分けなど「選ぶことに意味がある」問題は入力式でも選択式のまま
- **答える前は🔈を出さない**（産出問題では読み上げが答えそのものになるため）

## 並び順

分野も単元も**教科書と同じ順**です。「単元順に進む」を押すと、
教科書の最初の章から順に、まだ解き残しのある単元だけを出します。

| | インドネシア語 | 中国語 |
|---|---|---|
| 分野1 | 文字と音、あいさつ | 発音とあいさつ |
| 分野2 | 文の骨組み | 文の骨組み |
| 分野3 | 修飾と比較 | 量詞と修飾 |
| 分野4 | 時と数 | 時間と数 |
| 分野5 | 接辞 | 動詞まわり |
| 分野6 | 応用 | 会話 |

## FP版との違い

- **○×を使わない**。言語には向かないため、入力・4択・並べ替えの3形式
- **音声**：`speechSynthesis` で端末内蔵の音声を使う。追加ファイル不要、オフラインでも動く
  - 🔈ボタン、正解表示、表の1列目、間違えた問題の一覧で再生できる
  - 正解すると自動で読み上げる
  - 端末に該当言語の音声が入っていない場合は鳴らない（設定画面に状態を表示）
- **モード**：学科/実技のかわりに「文法 / 単語」。プール・弱点・記録・グラフがすべて追従

## そのまま引き継いだもの

教科書の閲覧、章ごとの読了、なぞって引くマーカー、ブックマーク、間隔をあけた復習テスト、
弱点の自動記録、得点推移の折れ線グラフ、中断した練習の再開、保存状態の表示、Claudeへの質問。

## 構成

```
lang/app.html          共通エンジン（1言語版のテンプレート。生成の元）
lang/index.html        2言語統合アプリ（生成物・公開しているもの）
lang/data/id/g1〜g3.js インドネシア語の教科書
lang/data/id/qg1,qg2.js 文法問題
lang/data/id/qv1.js    単語問題（単語リストから自動生成）
lang/data/id/qv2.js    単語問題・産出方向（入力式用に自動生成）
lang/data/zh/…         中国語も同じ構成（qg1 / qv1 / qv2）
id/index.html          旧・インドネシア語単独版（統合前。参照用に残置）
zh/index.html          旧・中国語単独版（同上）
```

`lang/index.html` は `lang/app.html` に差分を当てて `lang/data/**` を流し込んだ生成物です。
